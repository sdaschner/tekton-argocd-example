apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: run-systemtest
spec:
  params:
  - name: gateway
  resources:
    inputs:
    - name: source
      type: git
  steps:
  - name: run-mvn-st
    image: sdaschner/maven-jdk14:quarkus-deps-1.5.1-b5
    workingDir: "/workspace/source/system-test"
    command:
    - /usr/bin/mvn
    args:
    - verify
    - "-Dapp.test.host=$(inputs.params.gateway)"
    - "-Dapp.test.port=80"
    env:
    - name: MAVEN_OPTS
      value: "-Xms2G -Xmx4G"
    resources:
      requests:
        cpu: '2'
        memory: '4Gi'
---